# Generated by cloudcoil-model-codegen v0.7.1
# DO NOT EDIT

from typing import Annotated, Callable, ClassVar, Literal, overload

from cloudcoil.apimachinery import ObjectMeta
from cloudcoil.pydantic import (
    UNSET,
    BaseModel,
    BaseModelBuilder,
    BuilderContextBase,
    GenericListBuilder,
    ListBuilderContext,
    Never,
    Self,
)
from cloudcoil.resources import Resource
from pydantic import AwareDatetime, Field


class CertificateSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CertificateSpec"]:
            return CertificateSpec

        def build(self) -> "CertificateSpec":
            return CertificateSpec(**self._attrs)

        def dns_names(self, value: list[str], /) -> Self:
            "DNSNames is a list of DNS names the Certificate could support.\nThe wildcard format of DNSNames (e.g. *.default.example.com) is supported."
            return self._set("dns_names", value)

        def domain(self, value: str | None, /) -> Self:
            "Domain is the top level domain of the values for DNSNames."
            return self._set("domain", value)

        def secret_name(self, value: str, /) -> Self:
            "SecretName is the name of the secret resource to store the SSL certificate in."
            return self._set("secret_name", value)

    class BuilderContext(BuilderContextBase["CertificateSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CertificateSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CertificateSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["CertificateSpec", Builder]:
        return GenericListBuilder[CertificateSpec, CertificateSpec.Builder]()

    dns_names: Annotated[list[str], Field(alias="dnsNames")]
    "DNSNames is a list of DNS names the Certificate could support.\nThe wildcard format of DNSNames (e.g. *.default.example.com) is supported."
    domain: str | None = None
    "Domain is the top level domain of the values for DNSNames."
    secret_name: Annotated[str, Field(alias="secretName")]
    "SecretName is the name of the secret resource to store the SSL certificate in."


class ClusterDomainClaimSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ClusterDomainClaimSpec"]:
            return ClusterDomainClaimSpec

        def build(self) -> "ClusterDomainClaimSpec":
            return ClusterDomainClaimSpec(**self._attrs)

        def namespace(self, value: str, /) -> Self:
            "Namespace is the namespace which is allowed to create a DomainMapping\nusing this ClusterDomainClaim\u0027s name."
            return self._set("namespace", value)

    class BuilderContext(BuilderContextBase["ClusterDomainClaimSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ClusterDomainClaimSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ClusterDomainClaimSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ClusterDomainClaimSpec", Builder]:
        return GenericListBuilder[ClusterDomainClaimSpec, ClusterDomainClaimSpec.Builder]()

    namespace: str
    "Namespace is the namespace which is allowed to create a DomainMapping\nusing this ClusterDomainClaim\u0027s name."


class CertificateStatusConditionsItem(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CertificateStatusConditionsItem"]:
            return CertificateStatusConditionsItem

        def build(self) -> "CertificateStatusConditionsItem":
            return CertificateStatusConditionsItem(**self._attrs)

        def last_transition_time(self, value: str | None, /) -> Self:
            "LastTransitionTime is the last time the condition transitioned from one status to another.\nWe use VolatileTime in place of metav1.Time to exclude this from creating equality.Semantic\ndifferences (all other things held constant)."
            return self._set("last_transition_time", value)

        def message(self, value: str | None, /) -> Self:
            "A human readable message indicating details about the transition."
            return self._set("message", value)

        def reason(self, value: str | None, /) -> Self:
            "The reason for the condition\u0027s last transition."
            return self._set("reason", value)

        def severity(self, value: str | None, /) -> Self:
            "Severity with which to treat failures of this type of condition.\nWhen this is not specified, it defaults to Error."
            return self._set("severity", value)

        def status(self, value: str, /) -> Self:
            "Status of the condition, one of True, False, Unknown."
            return self._set("status", value)

        def type(self, value: str, /) -> Self:
            "Type of condition."
            return self._set("type", value)

    class BuilderContext(BuilderContextBase["CertificateStatusConditionsItem.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CertificateStatusConditionsItem.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CertificateStatusConditionsItem."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["CertificateStatusConditionsItem", Builder]:
        return GenericListBuilder[
            CertificateStatusConditionsItem, CertificateStatusConditionsItem.Builder
        ]()

    last_transition_time: Annotated[str | None, Field(alias="lastTransitionTime")] = None
    "LastTransitionTime is the last time the condition transitioned from one status to another.\nWe use VolatileTime in place of metav1.Time to exclude this from creating equality.Semantic\ndifferences (all other things held constant)."
    message: str | None = None
    "A human readable message indicating details about the transition."
    reason: str | None = None
    "The reason for the condition\u0027s last transition."
    severity: str | None = None
    "Severity with which to treat failures of this type of condition.\nWhen this is not specified, it defaults to Error."
    status: str
    "Status of the condition, one of True, False, Unknown."
    type: str
    "Type of condition."


class CertificateStatusHttp01ChallengesItem(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CertificateStatusHttp01ChallengesItem"]:
            return CertificateStatusHttp01ChallengesItem

        def build(self) -> "CertificateStatusHttp01ChallengesItem":
            return CertificateStatusHttp01ChallengesItem(**self._attrs)

        def service_name(self, value: str | None, /) -> Self:
            "ServiceName is the name of the service to serve HTTP01 challenge requests."
            return self._set("service_name", value)

        def service_namespace(self, value: str | None, /) -> Self:
            "ServiceNamespace is the namespace of the service to serve HTTP01 challenge requests."
            return self._set("service_namespace", value)

        def service_port(self, value: int | str | None, /) -> Self:
            "ServicePort is the port of the service to serve HTTP01 challenge requests."
            return self._set("service_port", value)

        def url(self, value: str | None, /) -> Self:
            "URL is the URL that the HTTP01 challenge is expected to serve on."
            return self._set("url", value)

    class BuilderContext(BuilderContextBase["CertificateStatusHttp01ChallengesItem.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CertificateStatusHttp01ChallengesItem.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CertificateStatusHttp01ChallengesItem."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["CertificateStatusHttp01ChallengesItem", Builder]:
        return GenericListBuilder[
            CertificateStatusHttp01ChallengesItem,
            CertificateStatusHttp01ChallengesItem.Builder,
        ]()

    service_name: Annotated[str | None, Field(alias="serviceName")] = None
    "ServiceName is the name of the service to serve HTTP01 challenge requests."
    service_namespace: Annotated[str | None, Field(alias="serviceNamespace")] = None
    "ServiceNamespace is the namespace of the service to serve HTTP01 challenge requests."
    service_port: Annotated[int | str | None, Field(alias="servicePort")] = None
    "ServicePort is the port of the service to serve HTTP01 challenge requests."
    url: str | None = None
    "URL is the URL that the HTTP01 challenge is expected to serve on."


class IngressSpecRulesItemHttpPathsItemHeadersValue(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["IngressSpecRulesItemHttpPathsItemHeadersValue"]:
            return IngressSpecRulesItemHttpPathsItemHeadersValue

        def build(self) -> "IngressSpecRulesItemHttpPathsItemHeadersValue":
            return IngressSpecRulesItemHttpPathsItemHeadersValue(**self._attrs)

        def exact(self, value: str, /) -> Self:
            return self._set("exact", value)

    class BuilderContext(
        BuilderContextBase["IngressSpecRulesItemHttpPathsItemHeadersValue.Builder"]
    ):
        def model_post_init(self, __context) -> None:
            self._builder = IngressSpecRulesItemHttpPathsItemHeadersValue.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for IngressSpecRulesItemHttpPathsItemHeadersValue."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["IngressSpecRulesItemHttpPathsItemHeadersValue", Builder]:
        return GenericListBuilder[
            IngressSpecRulesItemHttpPathsItemHeadersValue,
            IngressSpecRulesItemHttpPathsItemHeadersValue.Builder,
        ]()

    exact: str


class IngressSpecRulesItemHttpPathsItemSplitsItem(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["IngressSpecRulesItemHttpPathsItemSplitsItem"]:
            return IngressSpecRulesItemHttpPathsItemSplitsItem

        def build(self) -> "IngressSpecRulesItemHttpPathsItemSplitsItem":
            return IngressSpecRulesItemHttpPathsItemSplitsItem(**self._attrs)

        def append_headers(self, value: dict[str, str] | None, /) -> Self:
            "AppendHeaders allow specifying additional HTTP headers to add\nbefore forwarding a request to the destination service.\n\nNOTE: This differs from K8s Ingress which doesn\u0027t allow header appending."
            return self._set("append_headers", value)

        def percent(self, value: int | None, /) -> Self:
            "Specifies the split percentage, a number between 0 and 100.  If\nonly one split is specified, we default to 100.\n\nNOTE: This differs from K8s Ingress to allow percentage split."
            return self._set("percent", value)

        def service_name(self, value: str, /) -> Self:
            "Specifies the name of the referenced service."
            return self._set("service_name", value)

        def service_namespace(self, value: str, /) -> Self:
            "Specifies the namespace of the referenced service.\n\nNOTE: This differs from K8s Ingress to allow routing to different namespaces."
            return self._set("service_namespace", value)

        def service_port(self, value: int | str, /) -> Self:
            "Specifies the port of the referenced service."
            return self._set("service_port", value)

    class BuilderContext(BuilderContextBase["IngressSpecRulesItemHttpPathsItemSplitsItem.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = IngressSpecRulesItemHttpPathsItemSplitsItem.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for IngressSpecRulesItemHttpPathsItemSplitsItem."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["IngressSpecRulesItemHttpPathsItemSplitsItem", Builder]:
        return GenericListBuilder[
            IngressSpecRulesItemHttpPathsItemSplitsItem,
            IngressSpecRulesItemHttpPathsItemSplitsItem.Builder,
        ]()

    append_headers: Annotated[dict[str, str] | None, Field(alias="appendHeaders")] = None
    "AppendHeaders allow specifying additional HTTP headers to add\nbefore forwarding a request to the destination service.\n\nNOTE: This differs from K8s Ingress which doesn\u0027t allow header appending."
    percent: int | None = None
    "Specifies the split percentage, a number between 0 and 100.  If\nonly one split is specified, we default to 100.\n\nNOTE: This differs from K8s Ingress to allow percentage split."
    service_name: Annotated[str, Field(alias="serviceName")]
    "Specifies the name of the referenced service."
    service_namespace: Annotated[str, Field(alias="serviceNamespace")]
    "Specifies the namespace of the referenced service.\n\nNOTE: This differs from K8s Ingress to allow routing to different namespaces."
    service_port: Annotated[int | str, Field(alias="servicePort")]
    "Specifies the port of the referenced service."


class IngressSpecTlsItem(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["IngressSpecTlsItem"]:
            return IngressSpecTlsItem

        def build(self) -> "IngressSpecTlsItem":
            return IngressSpecTlsItem(**self._attrs)

        def hosts(self, value: list[str] | None, /) -> Self:
            "Hosts is a list of hosts included in the TLS certificate. The values in\nthis list must match the name/s used in the tlsSecret. Defaults to the\nwildcard host setting for the loadbalancer controller fulfilling this\nIngress, if left unspecified."
            return self._set("hosts", value)

        def secret_name(self, value: str | None, /) -> Self:
            "SecretName is the name of the secret used to terminate SSL traffic."
            return self._set("secret_name", value)

        def secret_namespace(self, value: str | None, /) -> Self:
            "SecretNamespace is the namespace of the secret used to terminate SSL traffic.\nIf not set the namespace should be assumed to be the same as the Ingress.\nIf set the secret should have the same namespace as the Ingress otherwise\nthe behaviour is undefined and not supported."
            return self._set("secret_namespace", value)

    class BuilderContext(BuilderContextBase["IngressSpecTlsItem.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = IngressSpecTlsItem.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for IngressSpecTlsItem."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["IngressSpecTlsItem", Builder]:
        return GenericListBuilder[IngressSpecTlsItem, IngressSpecTlsItem.Builder]()

    hosts: list[str] | None = None
    "Hosts is a list of hosts included in the TLS certificate. The values in\nthis list must match the name/s used in the tlsSecret. Defaults to the\nwildcard host setting for the loadbalancer controller fulfilling this\nIngress, if left unspecified."
    secret_name: Annotated[str | None, Field(alias="secretName")] = None
    "SecretName is the name of the secret used to terminate SSL traffic."
    secret_namespace: Annotated[str | None, Field(alias="secretNamespace")] = None
    "SecretNamespace is the namespace of the secret used to terminate SSL traffic.\nIf not set the namespace should be assumed to be the same as the Ingress.\nIf set the secret should have the same namespace as the Ingress otherwise\nthe behaviour is undefined and not supported."


class IngressStatusConditionsItem(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["IngressStatusConditionsItem"]:
            return IngressStatusConditionsItem

        def build(self) -> "IngressStatusConditionsItem":
            return IngressStatusConditionsItem(**self._attrs)

        def last_transition_time(self, value: str | None, /) -> Self:
            "LastTransitionTime is the last time the condition transitioned from one status to another.\nWe use VolatileTime in place of metav1.Time to exclude this from creating equality.Semantic\ndifferences (all other things held constant)."
            return self._set("last_transition_time", value)

        def message(self, value: str | None, /) -> Self:
            "A human readable message indicating details about the transition."
            return self._set("message", value)

        def reason(self, value: str | None, /) -> Self:
            "The reason for the condition\u0027s last transition."
            return self._set("reason", value)

        def severity(self, value: str | None, /) -> Self:
            "Severity with which to treat failures of this type of condition.\nWhen this is not specified, it defaults to Error."
            return self._set("severity", value)

        def status(self, value: str, /) -> Self:
            "Status of the condition, one of True, False, Unknown."
            return self._set("status", value)

        def type(self, value: str, /) -> Self:
            "Type of condition."
            return self._set("type", value)

    class BuilderContext(BuilderContextBase["IngressStatusConditionsItem.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = IngressStatusConditionsItem.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for IngressStatusConditionsItem."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["IngressStatusConditionsItem", Builder]:
        return GenericListBuilder[
            IngressStatusConditionsItem, IngressStatusConditionsItem.Builder
        ]()

    last_transition_time: Annotated[str | None, Field(alias="lastTransitionTime")] = None
    "LastTransitionTime is the last time the condition transitioned from one status to another.\nWe use VolatileTime in place of metav1.Time to exclude this from creating equality.Semantic\ndifferences (all other things held constant)."
    message: str | None = None
    "A human readable message indicating details about the transition."
    reason: str | None = None
    "The reason for the condition\u0027s last transition."
    severity: str | None = None
    "Severity with which to treat failures of this type of condition.\nWhen this is not specified, it defaults to Error."
    status: str
    "Status of the condition, one of True, False, Unknown."
    type: str
    "Type of condition."


class IngressStatusPrivateLoadBalancerIngressItem(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["IngressStatusPrivateLoadBalancerIngressItem"]:
            return IngressStatusPrivateLoadBalancerIngressItem

        def build(self) -> "IngressStatusPrivateLoadBalancerIngressItem":
            return IngressStatusPrivateLoadBalancerIngressItem(**self._attrs)

        def domain(self, value: str | None, /) -> Self:
            "Domain is set for load-balancer ingress points that are DNS based\n(typically AWS load-balancers)"
            return self._set("domain", value)

        def domain_internal(self, value: str | None, /) -> Self:
            "DomainInternal is set if there is a cluster-local DNS name to access the Ingress.\n\nNOTE: This differs from K8s Ingress, since we also desire to have a cluster-local\n      DNS name to allow routing in case of not having a mesh."
            return self._set("domain_internal", value)

        def ip(self, value: str | None, /) -> Self:
            "IP is set for load-balancer ingress points that are IP based\n(typically GCE or OpenStack load-balancers)"
            return self._set("ip", value)

        def mesh_only(self, value: bool | None, /) -> Self:
            "MeshOnly is set if the Ingress is only load-balanced through a Service mesh."
            return self._set("mesh_only", value)

    class BuilderContext(BuilderContextBase["IngressStatusPrivateLoadBalancerIngressItem.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = IngressStatusPrivateLoadBalancerIngressItem.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for IngressStatusPrivateLoadBalancerIngressItem."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["IngressStatusPrivateLoadBalancerIngressItem", Builder]:
        return GenericListBuilder[
            IngressStatusPrivateLoadBalancerIngressItem,
            IngressStatusPrivateLoadBalancerIngressItem.Builder,
        ]()

    domain: str | None = None
    "Domain is set for load-balancer ingress points that are DNS based\n(typically AWS load-balancers)"
    domain_internal: Annotated[str | None, Field(alias="domainInternal")] = None
    "DomainInternal is set if there is a cluster-local DNS name to access the Ingress.\n\nNOTE: This differs from K8s Ingress, since we also desire to have a cluster-local\n      DNS name to allow routing in case of not having a mesh."
    ip: str | None = None
    "IP is set for load-balancer ingress points that are IP based\n(typically GCE or OpenStack load-balancers)"
    mesh_only: Annotated[bool | None, Field(alias="meshOnly")] = None
    "MeshOnly is set if the Ingress is only load-balanced through a Service mesh."


class IngressStatusPublicLoadBalancerIngressItem(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["IngressStatusPublicLoadBalancerIngressItem"]:
            return IngressStatusPublicLoadBalancerIngressItem

        def build(self) -> "IngressStatusPublicLoadBalancerIngressItem":
            return IngressStatusPublicLoadBalancerIngressItem(**self._attrs)

        def domain(self, value: str | None, /) -> Self:
            "Domain is set for load-balancer ingress points that are DNS based\n(typically AWS load-balancers)"
            return self._set("domain", value)

        def domain_internal(self, value: str | None, /) -> Self:
            "DomainInternal is set if there is a cluster-local DNS name to access the Ingress.\n\nNOTE: This differs from K8s Ingress, since we also desire to have a cluster-local\n      DNS name to allow routing in case of not having a mesh."
            return self._set("domain_internal", value)

        def ip(self, value: str | None, /) -> Self:
            "IP is set for load-balancer ingress points that are IP based\n(typically GCE or OpenStack load-balancers)"
            return self._set("ip", value)

        def mesh_only(self, value: bool | None, /) -> Self:
            "MeshOnly is set if the Ingress is only load-balanced through a Service mesh."
            return self._set("mesh_only", value)

    class BuilderContext(BuilderContextBase["IngressStatusPublicLoadBalancerIngressItem.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = IngressStatusPublicLoadBalancerIngressItem.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for IngressStatusPublicLoadBalancerIngressItem."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["IngressStatusPublicLoadBalancerIngressItem", Builder]:
        return GenericListBuilder[
            IngressStatusPublicLoadBalancerIngressItem,
            IngressStatusPublicLoadBalancerIngressItem.Builder,
        ]()

    domain: str | None = None
    "Domain is set for load-balancer ingress points that are DNS based\n(typically AWS load-balancers)"
    domain_internal: Annotated[str | None, Field(alias="domainInternal")] = None
    "DomainInternal is set if there is a cluster-local DNS name to access the Ingress.\n\nNOTE: This differs from K8s Ingress, since we also desire to have a cluster-local\n      DNS name to allow routing in case of not having a mesh."
    ip: str | None = None
    "IP is set for load-balancer ingress points that are IP based\n(typically GCE or OpenStack load-balancers)"
    mesh_only: Annotated[bool | None, Field(alias="meshOnly")] = None
    "MeshOnly is set if the Ingress is only load-balanced through a Service mesh."


class ServerlessServiceSpecObjectRef(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ServerlessServiceSpecObjectRef"]:
            return ServerlessServiceSpecObjectRef

        def build(self) -> "ServerlessServiceSpecObjectRef":
            return ServerlessServiceSpecObjectRef(**self._attrs)

        def api_version(self, value: str | None, /) -> Self:
            "API version of the referent."
            return self._set("api_version", value)

        def field_path(self, value: str | None, /) -> Self:
            'If referring to a piece of an object instead of an entire object, this string\nshould contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].\nFor example, if the object reference is to a container within a pod, this would take on a value like:\n"spec.containers{name}" (where "name" refers to the name of the container that triggered\nthe event) or if no container name is specified "spec.containers[2]" (container with\nindex 2 in this pod). This syntax is chosen only to have some well-defined way of\nreferencing a part of an object.'
            return self._set("field_path", value)

        def kind(self, value: str | None, /) -> Self:
            "Kind of the referent.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        def name(self, value: str | None, /) -> Self:
            "Name of the referent.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
            return self._set("name", value)

        def namespace(self, value: str | None, /) -> Self:
            "Namespace of the referent.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/"
            return self._set("namespace", value)

        def resource_version(self, value: str | None, /) -> Self:
            "Specific resourceVersion to which this reference is made, if any.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency"
            return self._set("resource_version", value)

        def uid(self, value: str | None, /) -> Self:
            "UID of the referent.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids"
            return self._set("uid", value)

    class BuilderContext(BuilderContextBase["ServerlessServiceSpecObjectRef.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ServerlessServiceSpecObjectRef.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ServerlessServiceSpecObjectRef."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["ServerlessServiceSpecObjectRef", Builder]:
        return GenericListBuilder[
            ServerlessServiceSpecObjectRef, ServerlessServiceSpecObjectRef.Builder
        ]()

    api_version: Annotated[str | None, Field(alias="apiVersion")] = None
    "API version of the referent."
    field_path: Annotated[str | None, Field(alias="fieldPath")] = None
    'If referring to a piece of an object instead of an entire object, this string\nshould contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].\nFor example, if the object reference is to a container within a pod, this would take on a value like:\n"spec.containers{name}" (where "name" refers to the name of the container that triggered\nthe event) or if no container name is specified "spec.containers[2]" (container with\nindex 2 in this pod). This syntax is chosen only to have some well-defined way of\nreferencing a part of an object.'
    kind: str | None = None
    "Kind of the referent.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    name: str | None = None
    "Name of the referent.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names"
    namespace: str | None = None
    "Namespace of the referent.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/"
    resource_version: Annotated[str | None, Field(alias="resourceVersion")] = None
    "Specific resourceVersion to which this reference is made, if any.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency"
    uid: str | None = None
    "UID of the referent.\nMore info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids"


class ServerlessServiceStatusConditionsItem(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ServerlessServiceStatusConditionsItem"]:
            return ServerlessServiceStatusConditionsItem

        def build(self) -> "ServerlessServiceStatusConditionsItem":
            return ServerlessServiceStatusConditionsItem(**self._attrs)

        def last_transition_time(self, value: str | None, /) -> Self:
            "LastTransitionTime is the last time the condition transitioned from one status to another.\nWe use VolatileTime in place of metav1.Time to exclude this from creating equality.Semantic\ndifferences (all other things held constant)."
            return self._set("last_transition_time", value)

        def message(self, value: str | None, /) -> Self:
            "A human readable message indicating details about the transition."
            return self._set("message", value)

        def reason(self, value: str | None, /) -> Self:
            "The reason for the condition\u0027s last transition."
            return self._set("reason", value)

        def severity(self, value: str | None, /) -> Self:
            "Severity with which to treat failures of this type of condition.\nWhen this is not specified, it defaults to Error."
            return self._set("severity", value)

        def status(self, value: str, /) -> Self:
            "Status of the condition, one of True, False, Unknown."
            return self._set("status", value)

        def type(self, value: str, /) -> Self:
            "Type of condition."
            return self._set("type", value)

    class BuilderContext(BuilderContextBase["ServerlessServiceStatusConditionsItem.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ServerlessServiceStatusConditionsItem.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ServerlessServiceStatusConditionsItem."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["ServerlessServiceStatusConditionsItem", Builder]:
        return GenericListBuilder[
            ServerlessServiceStatusConditionsItem,
            ServerlessServiceStatusConditionsItem.Builder,
        ]()

    last_transition_time: Annotated[str | None, Field(alias="lastTransitionTime")] = None
    "LastTransitionTime is the last time the condition transitioned from one status to another.\nWe use VolatileTime in place of metav1.Time to exclude this from creating equality.Semantic\ndifferences (all other things held constant)."
    message: str | None = None
    "A human readable message indicating details about the transition."
    reason: str | None = None
    "The reason for the condition\u0027s last transition."
    severity: str | None = None
    "Severity with which to treat failures of this type of condition.\nWhen this is not specified, it defaults to Error."
    status: str
    "Status of the condition, one of True, False, Unknown."
    type: str
    "Type of condition."


class CertificateStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["CertificateStatus"]:
            return CertificateStatus

        def build(self) -> "CertificateStatus":
            return CertificateStatus(**self._attrs)

        def annotations(self, value: dict[str, str] | None, /) -> Self:
            "Annotations is additional Status fields for the Resource to save some\nadditional State as well as convey more information to the user. This is\nroughly akin to Annotations on any k8s resource, just the reconciler conveying\nricher information outwards."
            return self._set("annotations", value)

        @overload
        def conditions(
            self, value_or_callback: list[CertificateStatusConditionsItem] | None, /
        ) -> "CertificateStatus.Builder": ...

        @overload
        def conditions(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        CertificateStatusConditionsItem,
                        CertificateStatusConditionsItem.Builder,
                    ]
                ],
                GenericListBuilder[
                    CertificateStatusConditionsItem,
                    CertificateStatusConditionsItem.Builder,
                ]
                | list[CertificateStatusConditionsItem],
            ],
            /,
        ) -> "CertificateStatus.Builder": ...

        @overload
        def conditions(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[CertificateStatusConditionsItem.Builder]: ...

        def conditions(self, value_or_callback=UNSET, /):
            "Conditions the latest available observations of a resource\u0027s current state."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[CertificateStatusConditionsItem.Builder]()
                context._parent_builder = self
                context._field_name = "conditions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CertificateStatusConditionsItem.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("conditions", value)

        @overload
        def http01_challenges(
            self,
            value_or_callback: list[CertificateStatusHttp01ChallengesItem] | None,
            /,
        ) -> "CertificateStatus.Builder": ...

        @overload
        def http01_challenges(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        CertificateStatusHttp01ChallengesItem,
                        CertificateStatusHttp01ChallengesItem.Builder,
                    ]
                ],
                GenericListBuilder[
                    CertificateStatusHttp01ChallengesItem,
                    CertificateStatusHttp01ChallengesItem.Builder,
                ]
                | list[CertificateStatusHttp01ChallengesItem],
            ],
            /,
        ) -> "CertificateStatus.Builder": ...

        @overload
        def http01_challenges(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[CertificateStatusHttp01ChallengesItem.Builder]: ...

        def http01_challenges(self, value_or_callback=UNSET, /):
            "HTTP01Challenges is a list of HTTP01 challenges that need to be fulfilled\nin order to get the TLS certificate.."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[CertificateStatusHttp01ChallengesItem.Builder]()
                context._parent_builder = self
                context._field_name = "http01_challenges"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CertificateStatusHttp01ChallengesItem.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("http01_challenges", value)

        def not_after(self, value: AwareDatetime | None, /) -> Self:
            "The expiration time of the TLS certificate stored in the secret named\nby this resource in spec.secretName."
            return self._set("not_after", value)

        def observed_generation(self, value: int | None, /) -> Self:
            "ObservedGeneration is the \u0027Generation\u0027 of the Service that\nwas last processed by the controller."
            return self._set("observed_generation", value)

    class BuilderContext(BuilderContextBase["CertificateStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = CertificateStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for CertificateStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["CertificateStatus", Builder]:
        return GenericListBuilder[CertificateStatus, CertificateStatus.Builder]()

    annotations: dict[str, str] | None = None
    "Annotations is additional Status fields for the Resource to save some\nadditional State as well as convey more information to the user. This is\nroughly akin to Annotations on any k8s resource, just the reconciler conveying\nricher information outwards."
    conditions: list[CertificateStatusConditionsItem] | None = None
    "Conditions the latest available observations of a resource\u0027s current state."
    http01_challenges: Annotated[
        list[CertificateStatusHttp01ChallengesItem] | None,
        Field(alias="http01Challenges"),
    ] = None
    "HTTP01Challenges is a list of HTTP01 challenges that need to be fulfilled\nin order to get the TLS certificate.."
    not_after: Annotated[AwareDatetime | None, Field(alias="notAfter")] = None
    "The expiration time of the TLS certificate stored in the secret named\nby this resource in spec.secretName."
    observed_generation: Annotated[int | None, Field(alias="observedGeneration")] = None
    "ObservedGeneration is the \u0027Generation\u0027 of the Service that\nwas last processed by the controller."


class ClusterDomainClaim(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "clusterdomainclaims",
        "scope": "Cluster",
        "status": True,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ClusterDomainClaim"]:
            return ClusterDomainClaim

        def build(self) -> "ClusterDomainClaim":
            return ClusterDomainClaim(**self._attrs)

        def api_version(
            self, value: Literal["networking.internal.knative.dev/v1alpha1"], /
        ) -> Self:
            "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["ClusterDomainClaim"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(
            self, value_or_callback: ObjectMeta | None, /
        ) -> "ClusterDomainClaim.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "ClusterDomainClaim.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(
            self, value_or_callback: ClusterDomainClaimSpec | None, /
        ) -> "ClusterDomainClaim.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [ClusterDomainClaimSpec.Builder],
                ClusterDomainClaimSpec.Builder | ClusterDomainClaimSpec,
            ],
            /,
        ) -> "ClusterDomainClaim.Builder": ...

        @overload
        def spec(
            self, value_or_callback: Never = ...
        ) -> "ClusterDomainClaimSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            if self._in_context and value_or_callback is UNSET:
                context = ClusterDomainClaimSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ClusterDomainClaimSpec.builder())
                if isinstance(output, ClusterDomainClaimSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

    class BuilderContext(BuilderContextBase["ClusterDomainClaim.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ClusterDomainClaim.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ClusterDomainClaim."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ClusterDomainClaim", Builder]:
        return GenericListBuilder[ClusterDomainClaim, ClusterDomainClaim.Builder]()

    api_version: Annotated[
        Literal["networking.internal.knative.dev/v1alpha1"], Field(alias="apiVersion")
    ] = "networking.internal.knative.dev/v1alpha1"
    "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["ClusterDomainClaim"] = "ClusterDomainClaim"
    "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    spec: ClusterDomainClaimSpec | None = None


class ServerlessServiceSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ServerlessServiceSpec"]:
            return ServerlessServiceSpec

        def build(self) -> "ServerlessServiceSpec":
            return ServerlessServiceSpec(**self._attrs)

        def mode(self, value: str | None, /) -> Self:
            "Mode describes the mode of operation of the ServerlessService."
            return self._set("mode", value)

        def num_activators(self, value: int | None, /) -> Self:
            "NumActivators contains number of Activators that this revision should be\nassigned.\nO means \u2014 assign all."
            return self._set("num_activators", value)

        @overload
        def object_ref(
            self, value_or_callback: ServerlessServiceSpecObjectRef, /
        ) -> "ServerlessServiceSpec.Builder": ...

        @overload
        def object_ref(
            self,
            value_or_callback: Callable[
                [ServerlessServiceSpecObjectRef.Builder],
                ServerlessServiceSpecObjectRef.Builder | ServerlessServiceSpecObjectRef,
            ],
            /,
        ) -> "ServerlessServiceSpec.Builder": ...

        @overload
        def object_ref(
            self, value_or_callback: Never = ...
        ) -> "ServerlessServiceSpecObjectRef.BuilderContext": ...

        def object_ref(self, value_or_callback=UNSET, /):
            'ObjectRef defines the resource that this ServerlessService\nis responsible for making "serverless".'
            if self._in_context and value_or_callback is UNSET:
                context = ServerlessServiceSpecObjectRef.BuilderContext()
                context._parent_builder = self
                context._field_name = "object_ref"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ServerlessServiceSpecObjectRef.builder())
                if isinstance(output, ServerlessServiceSpecObjectRef.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("object_ref", value)

        def protocol_type(self, value: str, /) -> Self:
            "The application-layer protocol. Matches `RevisionProtocolType` set on the owning pa/revision.\nserving imports networking, so just use string."
            return self._set("protocol_type", value)

    class BuilderContext(BuilderContextBase["ServerlessServiceSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ServerlessServiceSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ServerlessServiceSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ServerlessServiceSpec", Builder]:
        return GenericListBuilder[ServerlessServiceSpec, ServerlessServiceSpec.Builder]()

    mode: str | None = None
    "Mode describes the mode of operation of the ServerlessService."
    num_activators: Annotated[int | None, Field(alias="numActivators")] = None
    "NumActivators contains number of Activators that this revision should be\nassigned.\nO means \u2014 assign all."
    object_ref: Annotated[ServerlessServiceSpecObjectRef, Field(alias="objectRef")]
    'ObjectRef defines the resource that this ServerlessService\nis responsible for making "serverless".'
    protocol_type: Annotated[str, Field(alias="protocolType")]
    "The application-layer protocol. Matches `RevisionProtocolType` set on the owning pa/revision.\nserving imports networking, so just use string."


class ServerlessServiceStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ServerlessServiceStatus"]:
            return ServerlessServiceStatus

        def build(self) -> "ServerlessServiceStatus":
            return ServerlessServiceStatus(**self._attrs)

        def annotations(self, value: dict[str, str] | None, /) -> Self:
            "Annotations is additional Status fields for the Resource to save some\nadditional State as well as convey more information to the user. This is\nroughly akin to Annotations on any k8s resource, just the reconciler conveying\nricher information outwards."
            return self._set("annotations", value)

        @overload
        def conditions(
            self,
            value_or_callback: list[ServerlessServiceStatusConditionsItem] | None,
            /,
        ) -> "ServerlessServiceStatus.Builder": ...

        @overload
        def conditions(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        ServerlessServiceStatusConditionsItem,
                        ServerlessServiceStatusConditionsItem.Builder,
                    ]
                ],
                GenericListBuilder[
                    ServerlessServiceStatusConditionsItem,
                    ServerlessServiceStatusConditionsItem.Builder,
                ]
                | list[ServerlessServiceStatusConditionsItem],
            ],
            /,
        ) -> "ServerlessServiceStatus.Builder": ...

        @overload
        def conditions(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[ServerlessServiceStatusConditionsItem.Builder]: ...

        def conditions(self, value_or_callback=UNSET, /):
            "Conditions the latest available observations of a resource\u0027s current state."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[ServerlessServiceStatusConditionsItem.Builder]()
                context._parent_builder = self
                context._field_name = "conditions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ServerlessServiceStatusConditionsItem.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("conditions", value)

        def observed_generation(self, value: int | None, /) -> Self:
            "ObservedGeneration is the \u0027Generation\u0027 of the Service that\nwas last processed by the controller."
            return self._set("observed_generation", value)

        def private_service_name(self, value: str | None, /) -> Self:
            "PrivateServiceName holds the name of a core K8s Service resource that\nload balances over the user service pods backing this Revision."
            return self._set("private_service_name", value)

        def service_name(self, value: str | None, /) -> Self:
            "ServiceName holds the name of a core K8s Service resource that\nload balances over the pods backing this Revision (activator or revision)."
            return self._set("service_name", value)

    class BuilderContext(BuilderContextBase["ServerlessServiceStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ServerlessServiceStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ServerlessServiceStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ServerlessServiceStatus", Builder]:
        return GenericListBuilder[ServerlessServiceStatus, ServerlessServiceStatus.Builder]()

    annotations: dict[str, str] | None = None
    "Annotations is additional Status fields for the Resource to save some\nadditional State as well as convey more information to the user. This is\nroughly akin to Annotations on any k8s resource, just the reconciler conveying\nricher information outwards."
    conditions: list[ServerlessServiceStatusConditionsItem] | None = None
    "Conditions the latest available observations of a resource\u0027s current state."
    observed_generation: Annotated[int | None, Field(alias="observedGeneration")] = None
    "ObservedGeneration is the \u0027Generation\u0027 of the Service that\nwas last processed by the controller."
    private_service_name: Annotated[str | None, Field(alias="privateServiceName")] = None
    "PrivateServiceName holds the name of a core K8s Service resource that\nload balances over the user service pods backing this Revision."
    service_name: Annotated[str | None, Field(alias="serviceName")] = None
    "ServiceName holds the name of a core K8s Service resource that\nload balances over the pods backing this Revision (activator or revision)."


class IngressSpecRulesItemHttpPathsItem(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["IngressSpecRulesItemHttpPathsItem"]:
            return IngressSpecRulesItemHttpPathsItem

        def build(self) -> "IngressSpecRulesItemHttpPathsItem":
            return IngressSpecRulesItemHttpPathsItem(**self._attrs)

        def append_headers(self, value: dict[str, str] | None, /) -> Self:
            "AppendHeaders allow specifying additional HTTP headers to add\nbefore forwarding a request to the destination service.\n\nNOTE: This differs from K8s Ingress which doesn\u0027t allow header appending."
            return self._set("append_headers", value)

        def headers(
            self,
            value: dict[str, IngressSpecRulesItemHttpPathsItemHeadersValue] | None,
            /,
        ) -> Self:
            "Headers defines header matching rules which is a map from a header name\nto HeaderMatch which specify a matching condition.\nWhen a request matched with all the header matching rules,\nthe request is routed by the corresponding ingress rule.\nIf it is empty, the headers are not used for matching"
            return self._set("headers", value)

        def path(self, value: str | None, /) -> Self:
            'Path represents a literal prefix to which this rule should apply.\nCurrently it can contain characters disallowed from the conventional\n"path" part of a URL as defined by RFC 3986. Paths must begin with\na \u0027/\u0027. If unspecified, the path defaults to a catch all sending\ntraffic to the backend.'
            return self._set("path", value)

        def rewrite_host(self, value: str | None, /) -> Self:
            "RewriteHost rewrites the incoming request\u0027s host header.\n\nThis field is currently experimental and not supported by all Ingress\nimplementations."
            return self._set("rewrite_host", value)

        @overload
        def splits(
            self,
            value_or_callback: list[IngressSpecRulesItemHttpPathsItemSplitsItem],
            /,
        ) -> "IngressSpecRulesItemHttpPathsItem.Builder": ...

        @overload
        def splits(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        IngressSpecRulesItemHttpPathsItemSplitsItem,
                        IngressSpecRulesItemHttpPathsItemSplitsItem.Builder,
                    ]
                ],
                GenericListBuilder[
                    IngressSpecRulesItemHttpPathsItemSplitsItem,
                    IngressSpecRulesItemHttpPathsItemSplitsItem.Builder,
                ]
                | list[IngressSpecRulesItemHttpPathsItemSplitsItem],
            ],
            /,
        ) -> "IngressSpecRulesItemHttpPathsItem.Builder": ...

        @overload
        def splits(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[IngressSpecRulesItemHttpPathsItemSplitsItem.Builder]: ...

        def splits(self, value_or_callback=UNSET, /):
            "Splits defines the referenced service endpoints to which the traffic\nwill be forwarded to."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[IngressSpecRulesItemHttpPathsItemSplitsItem.Builder]()
                context._parent_builder = self
                context._field_name = "splits"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(
                    IngressSpecRulesItemHttpPathsItemSplitsItem.list_builder()
                )
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("splits", value)

    class BuilderContext(BuilderContextBase["IngressSpecRulesItemHttpPathsItem.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = IngressSpecRulesItemHttpPathsItem.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for IngressSpecRulesItemHttpPathsItem."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["IngressSpecRulesItemHttpPathsItem", Builder]:
        return GenericListBuilder[
            IngressSpecRulesItemHttpPathsItem, IngressSpecRulesItemHttpPathsItem.Builder
        ]()

    append_headers: Annotated[dict[str, str] | None, Field(alias="appendHeaders")] = None
    "AppendHeaders allow specifying additional HTTP headers to add\nbefore forwarding a request to the destination service.\n\nNOTE: This differs from K8s Ingress which doesn\u0027t allow header appending."
    headers: dict[str, IngressSpecRulesItemHttpPathsItemHeadersValue] | None = None
    "Headers defines header matching rules which is a map from a header name\nto HeaderMatch which specify a matching condition.\nWhen a request matched with all the header matching rules,\nthe request is routed by the corresponding ingress rule.\nIf it is empty, the headers are not used for matching"
    path: str | None = None
    'Path represents a literal prefix to which this rule should apply.\nCurrently it can contain characters disallowed from the conventional\n"path" part of a URL as defined by RFC 3986. Paths must begin with\na \u0027/\u0027. If unspecified, the path defaults to a catch all sending\ntraffic to the backend.'
    rewrite_host: Annotated[str | None, Field(alias="rewriteHost")] = None
    "RewriteHost rewrites the incoming request\u0027s host header.\n\nThis field is currently experimental and not supported by all Ingress\nimplementations."
    splits: list[IngressSpecRulesItemHttpPathsItemSplitsItem]
    "Splits defines the referenced service endpoints to which the traffic\nwill be forwarded to."


class IngressStatusPrivateLoadBalancer(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["IngressStatusPrivateLoadBalancer"]:
            return IngressStatusPrivateLoadBalancer

        def build(self) -> "IngressStatusPrivateLoadBalancer":
            return IngressStatusPrivateLoadBalancer(**self._attrs)

        @overload
        def ingress(
            self,
            value_or_callback: list[IngressStatusPrivateLoadBalancerIngressItem] | None,
            /,
        ) -> "IngressStatusPrivateLoadBalancer.Builder": ...

        @overload
        def ingress(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        IngressStatusPrivateLoadBalancerIngressItem,
                        IngressStatusPrivateLoadBalancerIngressItem.Builder,
                    ]
                ],
                GenericListBuilder[
                    IngressStatusPrivateLoadBalancerIngressItem,
                    IngressStatusPrivateLoadBalancerIngressItem.Builder,
                ]
                | list[IngressStatusPrivateLoadBalancerIngressItem],
            ],
            /,
        ) -> "IngressStatusPrivateLoadBalancer.Builder": ...

        @overload
        def ingress(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[IngressStatusPrivateLoadBalancerIngressItem.Builder]: ...

        def ingress(self, value_or_callback=UNSET, /):
            "Ingress is a list containing ingress points for the load-balancer.\nTraffic intended for the service should be sent to these ingress points."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[IngressStatusPrivateLoadBalancerIngressItem.Builder]()
                context._parent_builder = self
                context._field_name = "ingress"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(
                    IngressStatusPrivateLoadBalancerIngressItem.list_builder()
                )
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("ingress", value)

    class BuilderContext(BuilderContextBase["IngressStatusPrivateLoadBalancer.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = IngressStatusPrivateLoadBalancer.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for IngressStatusPrivateLoadBalancer."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["IngressStatusPrivateLoadBalancer", Builder]:
        return GenericListBuilder[
            IngressStatusPrivateLoadBalancer, IngressStatusPrivateLoadBalancer.Builder
        ]()

    ingress: list[IngressStatusPrivateLoadBalancerIngressItem] | None = None
    "Ingress is a list containing ingress points for the load-balancer.\nTraffic intended for the service should be sent to these ingress points."


class IngressStatusPublicLoadBalancer(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["IngressStatusPublicLoadBalancer"]:
            return IngressStatusPublicLoadBalancer

        def build(self) -> "IngressStatusPublicLoadBalancer":
            return IngressStatusPublicLoadBalancer(**self._attrs)

        @overload
        def ingress(
            self,
            value_or_callback: list[IngressStatusPublicLoadBalancerIngressItem] | None,
            /,
        ) -> "IngressStatusPublicLoadBalancer.Builder": ...

        @overload
        def ingress(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        IngressStatusPublicLoadBalancerIngressItem,
                        IngressStatusPublicLoadBalancerIngressItem.Builder,
                    ]
                ],
                GenericListBuilder[
                    IngressStatusPublicLoadBalancerIngressItem,
                    IngressStatusPublicLoadBalancerIngressItem.Builder,
                ]
                | list[IngressStatusPublicLoadBalancerIngressItem],
            ],
            /,
        ) -> "IngressStatusPublicLoadBalancer.Builder": ...

        @overload
        def ingress(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[IngressStatusPublicLoadBalancerIngressItem.Builder]: ...

        def ingress(self, value_or_callback=UNSET, /):
            "Ingress is a list containing ingress points for the load-balancer.\nTraffic intended for the service should be sent to these ingress points."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[IngressStatusPublicLoadBalancerIngressItem.Builder]()
                context._parent_builder = self
                context._field_name = "ingress"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(
                    IngressStatusPublicLoadBalancerIngressItem.list_builder()
                )
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("ingress", value)

    class BuilderContext(BuilderContextBase["IngressStatusPublicLoadBalancer.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = IngressStatusPublicLoadBalancer.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for IngressStatusPublicLoadBalancer."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(
        cls,
    ) -> GenericListBuilder["IngressStatusPublicLoadBalancer", Builder]:
        return GenericListBuilder[
            IngressStatusPublicLoadBalancer, IngressStatusPublicLoadBalancer.Builder
        ]()

    ingress: list[IngressStatusPublicLoadBalancerIngressItem] | None = None
    "Ingress is a list containing ingress points for the load-balancer.\nTraffic intended for the service should be sent to these ingress points."


class Certificate(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "certificates",
        "scope": "Namespaced",
        "status": True,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Certificate"]:
            return Certificate

        def build(self) -> "Certificate":
            return Certificate(**self._attrs)

        def api_version(
            self, value: Literal["networking.internal.knative.dev/v1alpha1"], /
        ) -> Self:
            "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["Certificate"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "Certificate.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "Certificate.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(self, value_or_callback: CertificateSpec | None, /) -> "Certificate.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [CertificateSpec.Builder], CertificateSpec.Builder | CertificateSpec
            ],
            /,
        ) -> "Certificate.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "CertificateSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            if self._in_context and value_or_callback is UNSET:
                context = CertificateSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CertificateSpec.builder())
                if isinstance(output, CertificateSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

        @overload
        def status(
            self, value_or_callback: CertificateStatus | None, /
        ) -> "Certificate.Builder": ...

        @overload
        def status(
            self,
            value_or_callback: Callable[
                [CertificateStatus.Builder],
                CertificateStatus.Builder | CertificateStatus,
            ],
            /,
        ) -> "Certificate.Builder": ...

        @overload
        def status(self, value_or_callback: Never = ...) -> "CertificateStatus.BuilderContext": ...

        def status(self, value_or_callback=UNSET, /):
            if self._in_context and value_or_callback is UNSET:
                context = CertificateStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(CertificateStatus.builder())
                if isinstance(output, CertificateStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("status", value)

    class BuilderContext(BuilderContextBase["Certificate.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Certificate.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Certificate."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Certificate", Builder]:
        return GenericListBuilder[Certificate, Certificate.Builder]()

    api_version: Annotated[
        Literal["networking.internal.knative.dev/v1alpha1"], Field(alias="apiVersion")
    ] = "networking.internal.knative.dev/v1alpha1"
    "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["Certificate"] = "Certificate"
    "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    spec: CertificateSpec | None = None
    status: CertificateStatus | None = None


class IngressStatus(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["IngressStatus"]:
            return IngressStatus

        def build(self) -> "IngressStatus":
            return IngressStatus(**self._attrs)

        def annotations(self, value: dict[str, str] | None, /) -> Self:
            "Annotations is additional Status fields for the Resource to save some\nadditional State as well as convey more information to the user. This is\nroughly akin to Annotations on any k8s resource, just the reconciler conveying\nricher information outwards."
            return self._set("annotations", value)

        @overload
        def conditions(
            self, value_or_callback: list[IngressStatusConditionsItem] | None, /
        ) -> "IngressStatus.Builder": ...

        @overload
        def conditions(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        IngressStatusConditionsItem, IngressStatusConditionsItem.Builder
                    ]
                ],
                GenericListBuilder[IngressStatusConditionsItem, IngressStatusConditionsItem.Builder]
                | list[IngressStatusConditionsItem],
            ],
            /,
        ) -> "IngressStatus.Builder": ...

        @overload
        def conditions(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[IngressStatusConditionsItem.Builder]: ...

        def conditions(self, value_or_callback=UNSET, /):
            "Conditions the latest available observations of a resource\u0027s current state."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[IngressStatusConditionsItem.Builder]()
                context._parent_builder = self
                context._field_name = "conditions"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(IngressStatusConditionsItem.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("conditions", value)

        def observed_generation(self, value: int | None, /) -> Self:
            "ObservedGeneration is the \u0027Generation\u0027 of the Service that\nwas last processed by the controller."
            return self._set("observed_generation", value)

        @overload
        def private_load_balancer(
            self, value_or_callback: IngressStatusPrivateLoadBalancer | None, /
        ) -> "IngressStatus.Builder": ...

        @overload
        def private_load_balancer(
            self,
            value_or_callback: Callable[
                [IngressStatusPrivateLoadBalancer.Builder],
                IngressStatusPrivateLoadBalancer.Builder | IngressStatusPrivateLoadBalancer,
            ],
            /,
        ) -> "IngressStatus.Builder": ...

        @overload
        def private_load_balancer(
            self, value_or_callback: Never = ...
        ) -> "IngressStatusPrivateLoadBalancer.BuilderContext": ...

        def private_load_balancer(self, value_or_callback=UNSET, /):
            "PrivateLoadBalancer contains the current status of the load-balancer."
            if self._in_context and value_or_callback is UNSET:
                context = IngressStatusPrivateLoadBalancer.BuilderContext()
                context._parent_builder = self
                context._field_name = "private_load_balancer"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(IngressStatusPrivateLoadBalancer.builder())
                if isinstance(output, IngressStatusPrivateLoadBalancer.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("private_load_balancer", value)

        @overload
        def public_load_balancer(
            self, value_or_callback: IngressStatusPublicLoadBalancer | None, /
        ) -> "IngressStatus.Builder": ...

        @overload
        def public_load_balancer(
            self,
            value_or_callback: Callable[
                [IngressStatusPublicLoadBalancer.Builder],
                IngressStatusPublicLoadBalancer.Builder | IngressStatusPublicLoadBalancer,
            ],
            /,
        ) -> "IngressStatus.Builder": ...

        @overload
        def public_load_balancer(
            self, value_or_callback: Never = ...
        ) -> "IngressStatusPublicLoadBalancer.BuilderContext": ...

        def public_load_balancer(self, value_or_callback=UNSET, /):
            "PublicLoadBalancer contains the current status of the load-balancer."
            if self._in_context and value_or_callback is UNSET:
                context = IngressStatusPublicLoadBalancer.BuilderContext()
                context._parent_builder = self
                context._field_name = "public_load_balancer"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(IngressStatusPublicLoadBalancer.builder())
                if isinstance(output, IngressStatusPublicLoadBalancer.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("public_load_balancer", value)

    class BuilderContext(BuilderContextBase["IngressStatus.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = IngressStatus.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for IngressStatus."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["IngressStatus", Builder]:
        return GenericListBuilder[IngressStatus, IngressStatus.Builder]()

    annotations: dict[str, str] | None = None
    "Annotations is additional Status fields for the Resource to save some\nadditional State as well as convey more information to the user. This is\nroughly akin to Annotations on any k8s resource, just the reconciler conveying\nricher information outwards."
    conditions: list[IngressStatusConditionsItem] | None = None
    "Conditions the latest available observations of a resource\u0027s current state."
    observed_generation: Annotated[int | None, Field(alias="observedGeneration")] = None
    "ObservedGeneration is the \u0027Generation\u0027 of the Service that\nwas last processed by the controller."
    private_load_balancer: Annotated[
        IngressStatusPrivateLoadBalancer | None, Field(alias="privateLoadBalancer")
    ] = None
    "PrivateLoadBalancer contains the current status of the load-balancer."
    public_load_balancer: Annotated[
        IngressStatusPublicLoadBalancer | None, Field(alias="publicLoadBalancer")
    ] = None
    "PublicLoadBalancer contains the current status of the load-balancer."


class ServerlessService(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "serverlessservices",
        "scope": "Namespaced",
        "status": True,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["ServerlessService"]:
            return ServerlessService

        def build(self) -> "ServerlessService":
            return ServerlessService(**self._attrs)

        def api_version(
            self, value: Literal["networking.internal.knative.dev/v1alpha1"], /
        ) -> Self:
            "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["ServerlessService"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(
            self, value_or_callback: ObjectMeta | None, /
        ) -> "ServerlessService.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "ServerlessService.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(
            self, value_or_callback: ServerlessServiceSpec | None, /
        ) -> "ServerlessService.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[
                [ServerlessServiceSpec.Builder],
                ServerlessServiceSpec.Builder | ServerlessServiceSpec,
            ],
            /,
        ) -> "ServerlessService.Builder": ...

        @overload
        def spec(
            self, value_or_callback: Never = ...
        ) -> "ServerlessServiceSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            if self._in_context and value_or_callback is UNSET:
                context = ServerlessServiceSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ServerlessServiceSpec.builder())
                if isinstance(output, ServerlessServiceSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

        @overload
        def status(
            self, value_or_callback: ServerlessServiceStatus | None, /
        ) -> "ServerlessService.Builder": ...

        @overload
        def status(
            self,
            value_or_callback: Callable[
                [ServerlessServiceStatus.Builder],
                ServerlessServiceStatus.Builder | ServerlessServiceStatus,
            ],
            /,
        ) -> "ServerlessService.Builder": ...

        @overload
        def status(
            self, value_or_callback: Never = ...
        ) -> "ServerlessServiceStatus.BuilderContext": ...

        def status(self, value_or_callback=UNSET, /):
            if self._in_context and value_or_callback is UNSET:
                context = ServerlessServiceStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ServerlessServiceStatus.builder())
                if isinstance(output, ServerlessServiceStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("status", value)

    class BuilderContext(BuilderContextBase["ServerlessService.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = ServerlessService.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for ServerlessService."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["ServerlessService", Builder]:
        return GenericListBuilder[ServerlessService, ServerlessService.Builder]()

    api_version: Annotated[
        Literal["networking.internal.knative.dev/v1alpha1"], Field(alias="apiVersion")
    ] = "networking.internal.knative.dev/v1alpha1"
    "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["ServerlessService"] = "ServerlessService"
    "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    spec: ServerlessServiceSpec | None = None
    status: ServerlessServiceStatus | None = None


class IngressSpecRulesItemHttp(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["IngressSpecRulesItemHttp"]:
            return IngressSpecRulesItemHttp

        def build(self) -> "IngressSpecRulesItemHttp":
            return IngressSpecRulesItemHttp(**self._attrs)

        @overload
        def paths(
            self, value_or_callback: list[IngressSpecRulesItemHttpPathsItem], /
        ) -> "IngressSpecRulesItemHttp.Builder": ...

        @overload
        def paths(
            self,
            value_or_callback: Callable[
                [
                    GenericListBuilder[
                        IngressSpecRulesItemHttpPathsItem,
                        IngressSpecRulesItemHttpPathsItem.Builder,
                    ]
                ],
                GenericListBuilder[
                    IngressSpecRulesItemHttpPathsItem,
                    IngressSpecRulesItemHttpPathsItem.Builder,
                ]
                | list[IngressSpecRulesItemHttpPathsItem],
            ],
            /,
        ) -> "IngressSpecRulesItemHttp.Builder": ...

        @overload
        def paths(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[IngressSpecRulesItemHttpPathsItem.Builder]: ...

        def paths(self, value_or_callback=UNSET, /):
            "A collection of paths that map requests to backends.\n\nIf they are multiple matching paths, the first match takes precedence."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[IngressSpecRulesItemHttpPathsItem.Builder]()
                context._parent_builder = self
                context._field_name = "paths"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(IngressSpecRulesItemHttpPathsItem.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("paths", value)

    class BuilderContext(BuilderContextBase["IngressSpecRulesItemHttp.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = IngressSpecRulesItemHttp.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for IngressSpecRulesItemHttp."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["IngressSpecRulesItemHttp", Builder]:
        return GenericListBuilder[IngressSpecRulesItemHttp, IngressSpecRulesItemHttp.Builder]()

    paths: list[IngressSpecRulesItemHttpPathsItem]
    "A collection of paths that map requests to backends.\n\nIf they are multiple matching paths, the first match takes precedence."


class IngressSpecRulesItem(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["IngressSpecRulesItem"]:
            return IngressSpecRulesItem

        def build(self) -> "IngressSpecRulesItem":
            return IngressSpecRulesItem(**self._attrs)

        def hosts(self, value: list[str] | None, /) -> Self:
            'Host is the fully qualified domain name of a network host, as defined\nby RFC 3986. Note the following deviations from the "host" part of the\nURI as defined in the RFC:\n1. IPs are not allowed. Currently a rule value can only apply to the\n\t  IP in the Spec of the parent .\n2. The `:` delimiter is not respected because ports are not allowed.\n\t  Currently the port of an Ingress is implicitly :80 for http and\n\t  :443 for https.\nBoth these may change in the future.\nIf the host is unspecified, the Ingress routes all traffic based on the\nspecified IngressRuleValue.\nIf multiple matching Hosts were provided, the first rule will take precedent.'
            return self._set("hosts", value)

        @overload
        def http(
            self, value_or_callback: IngressSpecRulesItemHttp | None, /
        ) -> "IngressSpecRulesItem.Builder": ...

        @overload
        def http(
            self,
            value_or_callback: Callable[
                [IngressSpecRulesItemHttp.Builder],
                IngressSpecRulesItemHttp.Builder | IngressSpecRulesItemHttp,
            ],
            /,
        ) -> "IngressSpecRulesItem.Builder": ...

        @overload
        def http(
            self, value_or_callback: Never = ...
        ) -> "IngressSpecRulesItemHttp.BuilderContext": ...

        def http(self, value_or_callback=UNSET, /):
            "HTTP represents a rule to apply against incoming requests. If the\nrule is satisfied, the request is routed to the specified backend."
            if self._in_context and value_or_callback is UNSET:
                context = IngressSpecRulesItemHttp.BuilderContext()
                context._parent_builder = self
                context._field_name = "http"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(IngressSpecRulesItemHttp.builder())
                if isinstance(output, IngressSpecRulesItemHttp.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("http", value)

        def visibility(self, value: str | None, /) -> Self:
            "Visibility signifies whether this rule should `ClusterLocal`. If it\u0027s not\nspecified then it defaults to `ExternalIP`."
            return self._set("visibility", value)

    class BuilderContext(BuilderContextBase["IngressSpecRulesItem.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = IngressSpecRulesItem.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for IngressSpecRulesItem."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["IngressSpecRulesItem", Builder]:
        return GenericListBuilder[IngressSpecRulesItem, IngressSpecRulesItem.Builder]()

    hosts: list[str] | None = None
    'Host is the fully qualified domain name of a network host, as defined\nby RFC 3986. Note the following deviations from the "host" part of the\nURI as defined in the RFC:\n1. IPs are not allowed. Currently a rule value can only apply to the\n\t  IP in the Spec of the parent .\n2. The `:` delimiter is not respected because ports are not allowed.\n\t  Currently the port of an Ingress is implicitly :80 for http and\n\t  :443 for https.\nBoth these may change in the future.\nIf the host is unspecified, the Ingress routes all traffic based on the\nspecified IngressRuleValue.\nIf multiple matching Hosts were provided, the first rule will take precedent.'
    http: IngressSpecRulesItemHttp | None = None
    "HTTP represents a rule to apply against incoming requests. If the\nrule is satisfied, the request is routed to the specified backend."
    visibility: str | None = None
    "Visibility signifies whether this rule should `ClusterLocal`. If it\u0027s not\nspecified then it defaults to `ExternalIP`."


class IngressSpec(BaseModel):
    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["IngressSpec"]:
            return IngressSpec

        def build(self) -> "IngressSpec":
            return IngressSpec(**self._attrs)

        def http_option(self, value: str | None, /) -> Self:
            "HTTPOption is the option of HTTP. It has the following two values:\n`HTTPOptionEnabled`, `HTTPOptionRedirected`"
            return self._set("http_option", value)

        @overload
        def rules(
            self, value_or_callback: list[IngressSpecRulesItem] | None, /
        ) -> "IngressSpec.Builder": ...

        @overload
        def rules(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[IngressSpecRulesItem, IngressSpecRulesItem.Builder]],
                GenericListBuilder[IngressSpecRulesItem, IngressSpecRulesItem.Builder]
                | list[IngressSpecRulesItem],
            ],
            /,
        ) -> "IngressSpec.Builder": ...

        @overload
        def rules(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[IngressSpecRulesItem.Builder]: ...

        def rules(self, value_or_callback=UNSET, /):
            "A list of host rules used to configure the Ingress."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[IngressSpecRulesItem.Builder]()
                context._parent_builder = self
                context._field_name = "rules"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(IngressSpecRulesItem.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("rules", value)

        @overload
        def tls(
            self, value_or_callback: list[IngressSpecTlsItem] | None, /
        ) -> "IngressSpec.Builder": ...

        @overload
        def tls(
            self,
            value_or_callback: Callable[
                [GenericListBuilder[IngressSpecTlsItem, IngressSpecTlsItem.Builder]],
                GenericListBuilder[IngressSpecTlsItem, IngressSpecTlsItem.Builder]
                | list[IngressSpecTlsItem],
            ],
            /,
        ) -> "IngressSpec.Builder": ...

        @overload
        def tls(
            self, value_or_callback: Never = ...
        ) -> ListBuilderContext[IngressSpecTlsItem.Builder]: ...

        def tls(self, value_or_callback=UNSET, /):
            "TLS configuration. Currently Ingress only supports a single TLS\nport: 443. If multiple members of this list specify different hosts, they\nwill be multiplexed on the same port according to the hostname specified\nthrough the SNI TLS extension, if the ingress controller fulfilling the\ningress supports SNI."
            if self._in_context and value_or_callback is UNSET:
                context = ListBuilderContext[IngressSpecTlsItem.Builder]()
                context._parent_builder = self
                context._field_name = "tls"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(IngressSpecTlsItem.list_builder())
                if isinstance(output, GenericListBuilder):
                    value = output.build()
                else:
                    value = output
            return self._set("tls", value)

    class BuilderContext(BuilderContextBase["IngressSpec.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = IngressSpec.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for IngressSpec."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["IngressSpec", Builder]:
        return GenericListBuilder[IngressSpec, IngressSpec.Builder]()

    http_option: Annotated[str | None, Field(alias="httpOption")] = None
    "HTTPOption is the option of HTTP. It has the following two values:\n`HTTPOptionEnabled`, `HTTPOptionRedirected`"
    rules: list[IngressSpecRulesItem] | None = None
    "A list of host rules used to configure the Ingress."
    tls: list[IngressSpecTlsItem] | None = None
    "TLS configuration. Currently Ingress only supports a single TLS\nport: 443. If multiple members of this list specify different hosts, they\nwill be multiplexed on the same port according to the hostname specified\nthrough the SNI TLS extension, if the ingress controller fulfilling the\ningress supports SNI."


class Ingress(Resource):
    __cloudcoil_api__: ClassVar[dict] = {
        "plural": "ingresses",
        "scope": "Namespaced",
        "status": True,
    }

    class Builder(BaseModelBuilder):
        @property
        def cls(self) -> type["Ingress"]:
            return Ingress

        def build(self) -> "Ingress":
            return Ingress(**self._attrs)

        def api_version(
            self, value: Literal["networking.internal.knative.dev/v1alpha1"], /
        ) -> Self:
            "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
            return self._set("api_version", value)

        def kind(self, value: Literal["Ingress"], /) -> Self:
            "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
            return self._set("kind", value)

        @overload
        def metadata(self, value_or_callback: ObjectMeta | None, /) -> "Ingress.Builder": ...

        @overload
        def metadata(
            self,
            value_or_callback: Callable[[ObjectMeta.Builder], ObjectMeta.Builder | ObjectMeta],
            /,
        ) -> "Ingress.Builder": ...

        @overload
        def metadata(self, value_or_callback: Never = ...) -> "ObjectMeta.BuilderContext": ...

        def metadata(self, value_or_callback=UNSET, /):
            if self._in_context and value_or_callback is UNSET:
                context = ObjectMeta.BuilderContext()
                context._parent_builder = self
                context._field_name = "metadata"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(ObjectMeta.builder())
                if isinstance(output, ObjectMeta.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("metadata", value)

        @overload
        def spec(self, value_or_callback: IngressSpec | None, /) -> "Ingress.Builder": ...

        @overload
        def spec(
            self,
            value_or_callback: Callable[[IngressSpec.Builder], IngressSpec.Builder | IngressSpec],
            /,
        ) -> "Ingress.Builder": ...

        @overload
        def spec(self, value_or_callback: Never = ...) -> "IngressSpec.BuilderContext": ...

        def spec(self, value_or_callback=UNSET, /):
            if self._in_context and value_or_callback is UNSET:
                context = IngressSpec.BuilderContext()
                context._parent_builder = self
                context._field_name = "spec"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(IngressSpec.builder())
                if isinstance(output, IngressSpec.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("spec", value)

        @overload
        def status(self, value_or_callback: IngressStatus | None, /) -> "Ingress.Builder": ...

        @overload
        def status(
            self,
            value_or_callback: Callable[
                [IngressStatus.Builder], IngressStatus.Builder | IngressStatus
            ],
            /,
        ) -> "Ingress.Builder": ...

        @overload
        def status(self, value_or_callback: Never = ...) -> "IngressStatus.BuilderContext": ...

        def status(self, value_or_callback=UNSET, /):
            if self._in_context and value_or_callback is UNSET:
                context = IngressStatus.BuilderContext()
                context._parent_builder = self
                context._field_name = "status"
                return context

            if value_or_callback is UNSET:
                raise TypeError("A value is required outside a builder context")
            value = value_or_callback
            if callable(value_or_callback):
                output = value_or_callback(IngressStatus.builder())
                if isinstance(output, IngressStatus.Builder):
                    value = output.build()
                else:
                    value = output
            return self._set("status", value)

    class BuilderContext(BuilderContextBase["Ingress.Builder"]):
        def model_post_init(self, __context) -> None:
            self._builder = Ingress.Builder()
            self._builder._in_context = True
            self._parent_builder = None
            self._field_name = None

    @classmethod
    def builder(cls) -> Builder:
        return cls.Builder()

    @classmethod
    def new(cls) -> BuilderContext:
        """Creates a new context manager builder for Ingress."""
        return cls.BuilderContext()

    @classmethod
    def list_builder(cls) -> GenericListBuilder["Ingress", Builder]:
        return GenericListBuilder[Ingress, Ingress.Builder]()

    api_version: Annotated[
        Literal["networking.internal.knative.dev/v1alpha1"], Field(alias="apiVersion")
    ] = "networking.internal.knative.dev/v1alpha1"
    "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources"
    kind: Literal["Ingress"] = "Ingress"
    "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"
    metadata: ObjectMeta | None = None
    spec: IngressSpec | None = None
    status: IngressStatus | None = None
